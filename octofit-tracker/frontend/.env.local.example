## Copy this file to `.env.local` and set your Codespace name
# Required by the frontend to construct the API host used during dev runs
# Example:
# VITE_CODESPACE_NAME=my-codespace-123

VITE_CODESPACE_NAME=

# If this value is empty the app will use a safe local fallback:
# http://localhost:8000/api/<component>/
